'use client';
import { useMemo, useState } from 'react';
const money=(n:number)=>n.toLocaleString(undefined,{style:'currency',currency:'USD',maximumFractionDigits:0});
export default function EscrowShortageClient(){const [oldEscrow,setOldEscrow]=useState(450);const [tax,setTax]=useState(6000);const [insurance,setInsurance]=useState(1800);const [shortage,setShortage]=useState(1200);const [months,setMonths]=useState(12);const result=useMemo(()=>{const ongoing=(tax+insurance)/12;return {ongoing,repay:shortage/Math.max(1,months),delta:ongoing+shortage/Math.max(1,months)-oldEscrow};},[oldEscrow,tax,insurance,shortage,months]);return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-5 md:grid-cols-3"><label className="text-sm font-semibold">Old monthly escrow<input className="mt-1 w-full rounded-lg border p-2" type="number" min="0" value={oldEscrow} onChange={e=>setOldEscrow(Number(e.target.value))}/></label><label className="text-sm font-semibold">New annual property tax<input className="mt-1 w-full rounded-lg border p-2" type="number" min="0" value={tax} onChange={e=>setTax(Number(e.target.value))}/></label><label className="text-sm font-semibold">New annual insurance<input className="mt-1 w-full rounded-lg border p-2" type="number" min="0" value={insurance} onChange={e=>setInsurance(Number(e.target.value))}/></label><label className="text-sm font-semibold">Escrow shortage<input className="mt-1 w-full rounded-lg border p-2" type="number" min="0" value={shortage} onChange={e=>setShortage(Number(e.target.value))}/></label><label className="text-sm font-semibold">Repayment months<select className="mt-1 w-full rounded-lg border p-2" value={months} onChange={e=>setMonths(Number(e.target.value))}><option>12</option><option>6</option><option>24</option></select></label></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase text-slate-500">New monthly escrow</p><p className="text-2xl font-black">{money(result.ongoing)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase text-slate-500">Shortage repayment</p><p className="text-2xl font-black">{money(result.repay)}/mo</p></div><div className="rounded-xl bg-blue-50 p-4"><p className="text-xs uppercase text-blue-700">Estimated payment change</p><p className="text-2xl font-black">{result.delta>=0?'+':''}{money(result.delta)}/mo</p></div></div><p className="mt-5 rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm">After the shortage is repaid, the temporary repayment amount should fall, but the ongoing tax and insurance escrow can remain higher. Compare your servicer’s annual escrow analysis.</p></section>}
